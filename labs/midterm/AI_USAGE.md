# AI Usage Log — สอบกลางภาค

ชื่อ-รหัส: [ฐาปกรณ์ ใจงาม 68543210050-9]

บันทึกทุกครั้งที่ใช้ AI ระหว่างสอบ

| เวลา | งาน (B1/B2/B3/B4) | ถาม AI ว่าอะไร | ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|---|---|---|---|---|
| 11:15 | B1 | ถามสาเหตุที่ Console แจ้งว่า Each child in a list should have a unique key prop | ใช้คำแนะนำให้เพิ่ม `key={request.id}` ที่ `<RequestCard />` ภายใน `requests.map()` | ตรวจว่าแต่ละ request มี ID ไม่ซ้ำกัน บันทึกไฟล์ รีเฟรชหน้าเว็บ และตรวจสอบว่า Warning ใน Console หายไป |
| 11:25 | B2 | ถามสาเหตุที่ตัวเลขรอดำเนินการในแผงสรุปไม่ตรงกับรายการจริง | ใช้คำแนะนำให้ตรวจการคำนวณ `summary.pending` ใน `src/pages/DashboardPage.jsx` และเปลี่ยนเงื่อนไขเป็น `request.status === 'pending'` | เปรียบเทียบจำนวนในแผงสรุปกับข้อมูลคำร้องทั้ง 3 รายการ และตรวจว่ารอดำเนินการแสดงเป็น 1 รายการ |
| 11:35 | B3 | ถามสาเหตุที่กดตัวกรองรอดำเนินการแล้วแสดงคำร้องสถานะอื่น | ใช้คำแนะนำให้ตรวจ `filteredRequests` ใน `src/pages/DashboardPage.jsx` และเปลี่ยนเงื่อนไขจาก `request.status !== statusFilter` เป็น `request.status === statusFilter` | ทดลองกดตัวกรองทั้งหมด รอดำเนินการ กำลังดำเนินการ และเสร็จสิ้น แล้วตรวจว่ารายการที่แสดงตรงกับสถานะที่เลือก |
